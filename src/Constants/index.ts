export const DEPARTMENTS =[
    "Computer Science",
    +    "Physics",
    +    "History",
];

export const DEPARTMENTS_OPTIONS = DEPARTMENTS.map((item) => (
    {
        value: item,
        label: item,
    }
))